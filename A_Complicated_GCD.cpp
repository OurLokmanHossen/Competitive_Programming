#include <bits/stdc++.h>
using namespace std;
#define fastio() ios::sync_with_stdio(0); cin.tie(0);
#define ll long long

int main() {
    fastio();

    string a, b; cin >> a >> b;
    
   if (a == b) {
        cout << a << endl; 
    } else {
        cout << 1 << endl; 
    }
        
  
    return 0;
}
